Application: {{ app_name }}
Namespace: {{ namespace }}

This is judge evaluation for diagnosis iteration {{ iteration }}.

Current shared session state:
---
{{ shared_content }}
---

The shared session file is at: {{ shared_file }}

Evaluate only the most recent hypothesis (Iteration {{ iteration }}) above. Earlier iterations
are provided for context only. Verify the latest hypothesis against the cluster state, then call
approve_and_submit or reject_with_feedback with your verdict.
