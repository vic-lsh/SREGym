Application: {{ app_name }}
Namespace: {{ namespace }}

This is judge evaluation for mitigation iteration {{ iteration }}.

Current shared session state:
---
{{ shared_content }}
---

The shared session file is at: {{ shared_file }}

Evaluate only the most recent mitigation (Iteration {{ iteration }}) above. Earlier iterations
are provided for context only. Verify the latest mitigation against the cluster state, then call
approve_and_submit (with submission_ans="") or reject_with_feedback.
